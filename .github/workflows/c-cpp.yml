name: C/C++ CI

on:
  push:
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/checkout@v4
      with:
        repository: netdata/netdata
        path: netdata
    - name: install dependencies
      run: |
        sudo apt update -qq
        sudo apt install -qq libupsclient-dev
    - name: make
      run: |
        ls -l
        ls -l netdata
        ls -l netdata/src
        ls -l netdata/src/libnetdata/libnetdata.h
        clang -Wall -Inetdata/src main.c -lupsclient
